# Global Agent Skills

This directory is the **canonical storage** for global AI agent skills. All skills here are available across projects when symlinked into agent-specific directories.

## Purpose

- Central location for skills shared across Cursor, Claude Code, and GitHub Copilot
- Single source of truth — add or update a skill once, use everywhere via symlinks
- Managed by `npx skills add` CLI or manually

## Structure
