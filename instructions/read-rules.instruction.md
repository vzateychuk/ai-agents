---
description: Read Rules Instructions
alwaysApply: true
---

## At the Start of Each Session

1. **Locate all rule files** in the AI rules directory:

<user-home>/.agents/rules/*.md

2. Do not rely on internal glob tools; always use any OS-agnostic method to list files: e.g., directory listing via the OS, API, or AI internal file listing.

3. **Read all listed rules**.

4. **All rules in this directory are mandatory** and must be followed during the session.

5. **Merge these rules with the AI session rules**, using them as the authoritative guide for global AI session behavior.